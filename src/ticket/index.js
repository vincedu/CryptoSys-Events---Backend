const { resolvers } = require("./resolvers");
const { typeDefs } = require("./typeDefs");
const { AtomicAssetsAPI } = require("./atomicAssetsAPI");

module.exports = {
    resolvers,
    typeDefs,
    AtomicAssetsAPI,
};
